server:
  shutdown: graceful

spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s
  application:
    name: llm-rag-graph
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: ${NEO4J_PASSWORD:password}
  security:
    oauth2:
      resourceserver:
        jwt:
          # Shared Keycloak instance defined in llm-gateway's docker-compose; this service has its
          # own client (llm-rag-graph-client) in the same "llm-gateway" realm.
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8081/realms/llm-gateway}

app:
  meta:
    name: LLM RAG Graph
    version: 0.0.1-SNAPSHOT
    description: Graph RAG pipeline — Neo4j relationships + Anthropic Claude
    author: Himansu Nayak
    java-version: "21"
    spring-boot-version: "4.1.0"
    neo4j-version: "5.x"
    llm-model: claude-opus-4-8
    github: https://github.com/himansu/llm-rag-graph
  anthropic:
    api-key: ${ANTHROPIC_API_KEY:}
    model: claude-opus-4-8
    max-tokens: 4096
  graph:
    seed-data: ${GRAPH_SEED_DATA:false}
    context-depth: 4
    max-context-nodes: 20
  rag:
    evaluate-groundedness: ${EVALUATE_GROUNDEDNESS:false}  # adds one extra LLM call per generation
  security:
    auth-enabled: ${API_AUTH_ENABLED:true}   # secure by default; set API_AUTH_ENABLED=false for local dev
    allowed-origins: ${CORS_ALLOWED_ORIGINS:}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
      show-components: when-authorized
      roles: ACTUATOR
    info:
      enabled: true
  info:
    git:
      mode: full
    build:
      enabled: true
    env:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true
  tracing:
    sampling:
      probability: ${TRACING_SAMPLING:1.0}
  otlp:
    tracing:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4318}/v1/traces
  metrics:
    tags:
      application: ${spring.application.name}
    distribution:
      percentiles-histogram:
        http.server.requests: true
      slo:
        http.server.requests: 50ms,100ms,200ms,300ms,500ms,1s,2s,5s

resilience4j:
  circuitbreaker:
    instances:
      llm-rag-graph:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 15s
        permitted-number-of-calls-in-half-open-state: 3
        minimum-number-of-calls: 5
        register-health-indicator: true
  retry:
    instances:
      llm-rag-graph:
        max-attempts: 2
        wait-duration: 1s
        retry-exceptions:
          - java.io.IOException
          - org.springframework.web.client.ResourceAccessException
  ratelimiter:
    instances:
      rag-query:
        limit-for-period: 20
        limit-refresh-period: 1m
        timeout-duration: 0s

logging:
  level:
    com.org.llm: DEBUG
    org.neo4j.driver: WARN
    org.springframework.data.neo4j: DEBUG
